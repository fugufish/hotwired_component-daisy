# frozen_string_literal: true

source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in hotwired_component-daisy.gemspec.
gemspec

group :development do
  gem "sqlite3"
end

gem "capybara"
gem "rspec-rails"
gem "rubocop"
gem "simplecov"
gem "solargraph"
gem "yard"

# To use a debugger
# gem 'byebug', group: [:development, :test]
